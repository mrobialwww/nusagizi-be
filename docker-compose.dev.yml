# =============================================================================
# docker-compose.dev.yml — Development services
#
# Cara pakai:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
#
# Atau via Makefile:
#   make dev-up
# =============================================================================

services:
  postgres:
    # Di dev, port postgres diekspos ke localhost untuk debugging langsung
    ports:
      - "127.0.0.1:5433:5432"

  minio:
    # Di dev, kedua port MinIO diekspos untuk akses langsung dari host
    ports:
      - "127.0.0.1:9002:9000"
      - "127.0.0.1:9003:9001"

  # ---------------------------------------------------------------------------
  # Backend Development — didefinisikan penuh di sini, tidak ada di base
  # ---------------------------------------------------------------------------
  backend-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: nusagizi-backend:dev
    container_name: nusagizi-backend-dev
    restart: unless-stopped
    env_file:
      - .env.dev
    environment:
      APP_ENV: development
      PORT: 8080
      DATABASE_URL: postgres://${POSTGRES_USER:-nusagizi}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB_DEV:-nusagizi_dev}?sslmode=disable
      MINIO_ENDPOINT: minio:9000
    ports:
      - "127.0.0.1:8081:8080"
    volumes:
      # Mount source code untuk Air hot-reload
      - .:/app
      # Lindungi direktori tmp di container dari ditimpa mount di atas
      - /app/tmp
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - dev-network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          memory: 64M
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "3"
